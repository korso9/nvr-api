const { sendScoreEmail } = require('../utils/email');
const Score = require('../models/Score');

const postScore = () => {};
const getScore = () => {};

module.exports = {
  postScore,
  getScore,
};
