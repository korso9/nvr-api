const { sendScoreEmail } = require('../utils/email');
const Score = require('../models/Score');

const createNewScore = () => {};
const getScore = () => {};
const addScore = () => {};

module.exports = {
  createNewScore,
  getScore,
  addScore,
};
