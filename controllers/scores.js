const { sendScoreEmail } = require('../utils/email');
const Score = require('../models/Score');

const createNewScore = () => {};
const getScore = () => {};
const addScore = () => {};
const unlockScore = () => {};

module.exports = {
  createNewScore,
  getScore,
  addScore,
  unlockScore,
};
